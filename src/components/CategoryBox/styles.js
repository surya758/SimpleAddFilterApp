import { Colors } from "../../styles";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: { marginRight: 10, backgroundColor: Colors.WHITE, padding: 8, borderRadius: 8 },
	title: { fontSize: 12, fontFamily: "SourceSansPro_400Regular" },
});

export default styles;
